services:
  web:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/storage/db.sqlite
    volumes:
      - ./storage:/app/storage
    ports:
      - "127.0.0.1:${HOST_WEB_PORT}:3000"

  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/storage/db.sqlite
    volumes:
      - ./storage:/app/storage
    command: ["node", "bot.js"]
    depends_on:
      - web
