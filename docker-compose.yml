services:
  web:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/db.sqlite
    volumes:
      - ./db.sqlite:/app/db.sqlite
    ports:
      - "127.0.0.1:${HOST_WEB_PORT}:3000"

  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/db.sqlite
    volumes:
      - ./db.sqlite:/app/db.sqlite
    command: ["node", "bot.js"]
    depends_on:
      - web
